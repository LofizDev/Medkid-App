import React from 'react';

import {
    StyleSheet,
    Text,
    View,
} from 'react-native';

const MedicationDetails = (): JSX.Element => {

    return (
        <View>
            <Text style={styles.container}>
                MedicationDetails
            </Text >
        </View>

    );
};

export default MedicationDetails;

const styles = StyleSheet.create({
    container: {},
})