import React from 'react';

import {
    StyleSheet,
    Text,
    View,
} from 'react-native';

const AppointmentBooking = (): JSX.Element => {

    return (
        <View>
            <Text style={styles.container}>
                AppointmentBooking
            </Text >
        </View>

    );
};

export default AppointmentBooking;

const styles = StyleSheet.create({
    container: {},
})